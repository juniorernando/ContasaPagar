# Terraform AWS Provider e recursos principais
provider "aws" {
  region = var.region
}

# Recursos: VPC, Aurora, Lambdas, SQS, SNS, IAM, API Gateway, Secrets Manager
# (Adicionar recursos conforme detalhamento do projeto)
